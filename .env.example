# Optional. When absent, local order submissions open a pre-filled WhatsApp message.
PUBLIC_ORDER_WEBHOOK_URL=
# Private n8n webhook used only by the unlisted owner-upload page.
PUBLIC_CAKE_UPLOAD_WEBHOOK_URL=
